return {
  {
    "hrsh7th/nvim-cmp",
    opts = {
      experimental = {
        ghost_text = false,
      },
    },
    -- opts = function(_, opts)
    --   table.insert(opts.experimental, { ghost_text = false })
    -- end,
  },
}
